FLASK_APP=flaskr
FLASK_ENV=development

run:
	export FLASK_APP=$(FLASK_APP) && \
  export FLASK_ENV=developemtn && \
  flask run

init-db:
	export FLASK_APP=$(FLASK_APP) && \
  export FLASK_ENV=developemtn && \
  flask init-db
